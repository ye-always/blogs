export interface AdminConfig {
  title: string;
  logo: string;
  logoUrl?: string;
  logoIcon?: string;
}

export const adminConfig: AdminConfig = {
  title: '管理后台',
  logo: '🚀',
  logoUrl: '',
  logoIcon: 'rocket',
};